process GLOBALSTRIP =
{
   untracked PSet maxEvents = {untracked int32 input = 1}

   ## setup useful services
   ##
   include "Validation/GlobalHits/data/MessageLogger.cfi" 

   ## Step 1: Read in MC+Sim events
   ## 
   ## read in OscarProducer file
   include "Validation/GlobalHits/data/PoolSource.cfi"
   replace maxEvents.input = -1

   ## Accumulation of globally transformed data
   ##
   ##module globalhithistogram
   include "Validation/GlobalHits/data/globalhits_prodhiststrip.cff"
   replace globalhitprodhiststrip.Verbosity = 0
   replace globalhitprodhiststrip.Frequency = 1	

   ## execute path
   ##
   path p1 = { globalhitprodhiststrip }

}
