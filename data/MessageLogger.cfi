service = MessageLogger
{
   untracked vstring destinations =
   {
      "MessageLogger.log",
      "cout",
      "cerr"
   }

   untracked vstring fwkJobReports = 
   {
      "FrameworkJobReport.xml"
   }

   untracked vstring categories = 
   { 
      "FwkJob",
      "GlobalValProducer.GlobalValProducer",
      "GlobalValProducer.endJob",
      "GlobalValProducer.produce",
      "GlobalValProducer.fillG4MC",
      "GlobalValProducer.storeG4MC",
      "GlobalValProducer.fillTrk",
      "GlobalValProducer.storeTrk",
      "GlobalValProducer.fillMuon",
      "GlobalValProducer.storeMuon",
      "GlobalValProducer.fillECal",
      "GlobalValProducer.storeECal",
      "GlobalValProducer.fillHCal",
      "GlobalValProducer.storeHCal",
      "GlobalValProducer.clear",
      "ScheduleExecutionFailure",
      "EventSetupDependency" 
   }

   untracked PSet MessageLogger.log =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet GlobalValProducer.GlobalValProducer = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.endJob = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.produce = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.clear = { untracked int32 limit = 10000000 }
      untracked PSet ScheduleExecutionFailure = { untracked int32 limit = 10000000 }
      untracked PSet EventSetupDependency = { untracked int32 limit = 10000000 }
   }
   untracked PSet cout =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked string threshold = "INFO"
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet GlobalValProducer.GlobalValProducer = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.endJob = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.produce = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.clear = { untracked int32 limit = 10000000 }
      //untracked PSet ScheduleExecutionFailure = { untracked int32 limit = 10000000 }
      //untracked PSet EventSetupDependency = { untracked int32 limit = 10000000 }  
   }
   untracked PSet cerr =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked string threshold = "WARNING"
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet GlobalValProducer.GlobalValProducer = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.endJob = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.produce = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeG4MC = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeTrk = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeMuon = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeECal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.fillHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.storeHCal = { untracked int32 limit = 10000000 }
      untracked PSet GlobalValProducer.clear = { untracked int32 limit = 10000000 } 
      untracked PSet ScheduleExecutionFailure = { untracked int32 limit = 10000000 }
      untracked PSet EventSetupDependency = { untracked int32 limit = 10000000 } 
   }

   untracked PSet FrameworkJobReport.xml = 
   {
      //untracked PSet default = { untracked int32 limit = 0 }
      untracked PSet FwkJob = { untracked int32 limit = 10000000 }
   }
}